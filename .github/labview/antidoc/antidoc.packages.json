{
  "schemaVersion": 1,
  "name": "Antidoc capability VIPM dependencies",
  "description": "OPT-IN, capability-scoped VIPM dependency metadata for the Antidoc documentation generator. The committed antidoc.vipc is authoritative and is what image builds apply. This JSON remains useful for dashboards/config tooling and optional ad-hoc regeneration with build-tooling-vipc.py, but it is not the source of truth by itself. Antidoc is deliberately kept OUT of ci-tooling.vipc (the core set every action runner needs) so this capability and its dependency chain are baked only when the Antidoc workflow is present. Pattern for new VIPM-installed capabilities: give each one its own .github/labview/<capability>/<capability>.vipc and bake it only when that functionality is installed.",

  "target": {
    "name": "LabVIEW",
    "labviewVersion": "2026",
    "comment": "labviewVersion must match the LabVIEW in the NI base image. The generator maps it to the .vipc Target <Version> (2026 -> '26.1 (64-bit)')."
  },

  "vipmPackages": {
    "comment": "Top-level packages only. Each is emitted as a <Package Included=false> entry, so `vipm apply_vipc` downloads it (and its dependencies, listed here for reference only) from the configured VIPM repository at build time.",
    "packages": [
      {
        "name": "wovalab_lib_antidoc_cli",
        "version": "3.3.0.117",
        "include": true,
        "summary": "Antidoc CLI by Wovalab - generate LabVIEW project documentation headlessly (the supported CI/CD path).",
        "dependenciesAutoResolved": [
          "wovalab_lib_antidoc",
          "wovalab_lib_asciidoc_for_labview",
          "wiresmith_technology_lib_g_cli"
        ],
        "note": "Doc generation renders diagrams via a Kroki server at run time (Wovalab-hosted by default); only the doc-gen step needs that network, not the install."
      }
    ]
  }
}
